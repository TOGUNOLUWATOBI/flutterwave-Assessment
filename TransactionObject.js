const SplitType =
{
    Flat:"FLAT",
    Percentage:"PERCENTAGE",
    Ratio:"RATIO"
}

var split_type = "";
switch(split_type)
{
    case SplitType.Flat:
        {
            
            break;
        }

    case SplitType.Percentage:
        {

            break;
        }
    case SplitType.Ratio:
        {

            break;
        }
    default:
        console.log("split type doesn't exist")
}

